{
  "name": "@lts/extension",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs chrome && node scripts/build.mjs firefox",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@lts/protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.4",
    "esbuild": "^0.25.10",
    "typescript": "^5.9.2"
  }
}
